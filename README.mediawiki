
= Code Alignment — VSCode 代码对齐扩展 =

'''Code Alignment''' 是一款通过对齐分隔符来美化代码格式的 VSCode 扩展，能够显著提升代码的可读性。基于从数学和其他学科借鉴的原则，通过将相似数据在垂直方向按列对齐，赋予代码额外的视觉含义——就像我们使用 Excel 表格时自然会做的那样。

本项目基于原 Notepad++ / Visual Studio 版 Code Alignment 插件完整移植，保持了所有核心功能、交互逻辑和使用体验的一致。

欲了解更多信息、示例、问题追踪和变更日志，请访问 [http://www.codealignment.com Code Alignment 官方网站]。

== 安装方式 ==

=== 方式一：从 VSIX 包安装（推荐离线安装）===
# 打开 VSCode，按下 '''Ctrl+Shift+P''' 打开命令面板
# 输入并选择 '''Extensions: Install from VSIX...'''
# 在弹出的文件选择框中找到 <code>codealignment-1.0.0.vsix</code> 文件，点击确定
# 安装完成后重启 VSCode 即可生效

=== 方式二：从源码编译安装 ===
适合需要自行构建或二次开发的用户：

  cd vscode-extension
  npm install
  npm run compile
  npx vsce package
  # 生成的 .vsix 文件位于当前目录，然后用方式一安装

=== 方式三：从 VSCode 扩展市场安装 ===
在 VSCode 扩展面板（'''Ctrl+Shift+X'''）中搜索 "Code Alignment"，点击安装即可。

== 兼容性 ==

* VSCode 1.85.0 及以上版本均可正常运行
* 已在 VSCode 1.97.x（最新稳定版）、1.93.x、1.85.x 上通过验证

== 什么是代码对齐 ==

代码对齐（Code Alignment）是一种通过格式化代码使其在垂直方向上按列排布来提升可读性的实践。

'''对齐前：'''

  var name = "foo";
  var anotherName = "bar";
  var someVariable = "baz";

'''执行 "按等号对齐" 后：'''

  var name         = "foo";
  var anotherName  = "bar";
  var someVariable = "baz";

常见的借口是"太难了、太费时间、不值得"。有了这个扩展，这些借口都不再成立。

== 核心功能 ==

=== 对齐命令一览 ===

{| class="wikitable"
! 命令名称
! 功能说明
! 默认快捷键
|-
| '''Align by...'''
| 输入自定义分隔符字符串进行对齐
| Ctrl+Shift+=
|-
| '''Align by equals'''
| 按单个等号 <code>=</code> 对齐
| Ctrl+Shift+1
|-
| '''Align by equals equals'''
| 按双等号 <code>==</code> 对齐
| Ctrl+Shift+2
|-
| '''Align by m_'''
| 按成员变量前缀 <code>m_</code> 对齐
| Ctrl+Shift+3
|-
| '''Align by quote'''
| 按双引号 <code>"</code> 对齐
| Ctrl+Shift+4
|-
| '''Align by period'''
| 按句点 <code>.</code> 对齐
| Ctrl+Shift+5
|-
| '''Align by space'''
| 按空格对齐
| Ctrl+Shift+6
|-
| '''Align from caret'''
| 从光标位置开始对齐（弹出分隔符输入框）
| Ctrl+Shift+7
|-
| '''Align by key'''
| 按键对齐模式（交互式快捷对齐）
| Ctrl+=
|}

=== 功能入口 ===

您可以通过以下四种方式使用 Code Alignment：

* '''命令面板'''：按 '''Ctrl+Shift+P'''，输入 "Align" 即可看到所有对齐命令
* '''右键菜单'''：在编辑器任意位置右键，展开 "Code Alignment" 菜单组
* '''状态栏按钮'''：编辑器底部状态栏右侧有 "Align" 快捷按钮，点击即打开分隔符输入框
* '''键盘快捷键'''：使用上表中的默认快捷键直接触发对齐操作（可在 VSCode 键盘快捷方式设置中自定义）

=== 更多对齐示例 ===

'''按 <code>.</code> 对齐（常用于对象属性访问链）：'''

  someObject
      .Property1
      .AnotherProperty
      .FinalProperty;

'''按 <code>m_</code> 对齐（常用于类成员变量声明）：'''

  private string m_name;
  private int    m_age;
  private bool   m_isActive;

'''按 <code>=></code> 对齐（Lambda 表达式 / 箭头函数）：'''

  const add      = (a, b) => a + b;
  const multiply = (a, b) => a * b;
  const divide   = (a, b) => a / b;

== Align by key 按键对齐模式详解 ==

这是 Code Alignment 最强大的功能之一。按下 '''Ctrl+=''' 进入按键对齐模式：

# 弹出一个快捷选择面板，列出您已配置的所有快捷键及其对应的对齐分隔符
# 在面板中输入或选择对应键位字符（如按 <code>=</code> 键输入等号），即可执行该键位对应的对齐操作
# 每次对齐完成后，面板'''保持打开状态'''，支持链式连续对齐（Chain 模式）
# 点击面板右上角的 '''Align from Caret''' 按钮可从光标位置启动对齐
# 按 '''Esc''' 或点击 '''Exit''' 按钮退出按键对齐模式

=== 对齐链操作示例 ===

假设您有一段 C# 代码需要同时按等号和引号对齐：

  var name = "foo";
  var anotherName = "bar";
  var someVariable = "baz";

# 选中需要对齐的代码区域
# 按下 '''Ctrl+=''' 进入按键对齐模式
# 输入 <code>=</code> 或从列表中选择 → 等号对齐完成
# 面板保持打开，继续输入 <code>"</code> → 引号对齐也完成
# 按 '''Esc''' 退出

最终效果：

  var name         = "foo";
  var anotherName  = "bar";
  var someVariable = "baz";

== Verilog / SystemVerilog 对齐支持（v1.1 新增）==

本扩展新增了对 '''Verilog''' 和 '''SystemVerilog''' 硬件描述语言的原生对齐支持，适配数字 IC 设计领域的主流编码规范。

=== Verilog/SV 专用对齐命令 ===

{| class="wikitable"
! 命令名称
! 功能说明
|-
| '''VL: Align blocking assigns (=, add space)'''
| 按阻塞赋值等号 <code>=</code> 对齐，自动在等号前添加空格
|-
| '''VL: Align by non-blocking assign (&lt;=)'''
| 按非阻塞赋值符号 <code>&lt;=</code> 对齐（时序逻辑 always 块专用）
|-
| '''VL: Align port declarations (input/output)'''
| 按端口声明关键字 <code>input</code>/<code>output</code>/<code>inout</code> 对齐
|-
| '''VL: Align signal bit-width ([7:0])'''
| 按位宽声明左括号 <code>[</code> 对齐，统一整理端口和信号列表的位宽位置
|-
| '''VL: Align module instances (.port)'''
| 按模块实例化的命名端口 <code>.(</code> 对齐
|-
| '''VL: Align assign statements'''
| 按 <code>assign</code> 关键字对齐
|-
| '''VL: Align case items (:)'''
| 按 case 分支冒号对齐
|-
| '''VL: Align inline comments (//)'''
| 按行内注释 <code>//</code> 对齐
|}

=== 智能块边界检测（VerilogScopeSelector）===

Verilog/SV 文件中，当光标在单个行上（没有选中多行）时，扩展会自动识别语法块边界来确定对齐范围：

* '''module/endmodule''' — 识别模块声明边界
* '''always/always_comb/always_ff/always_latch''' — 识别过程块边界
* '''begin/end''' — 识别顺序块边界
* '''generate/endgenerate''' — 识别生成块边界
* '''interface/endinterface''' — 识别接口声明边界
* '''package/endpackage''' — 识别包声明边界
* '''fork/join''' — 识别并行块边界

=== Verilog/SV 对齐示例 ===

'''示例 1：端口声明对齐（执行 "VL: Align port declarations"）'''

对齐前：

  input wire        clk,
  input wire        rst_n,
  output reg [7:0]  data_out,
  output wire       valid,
  inout wire [15:0] bus_data

对齐后：

  input  wire        clk,
  input  wire        rst_n,
  output reg   [7:0] data_out,
  output wire        valid,
  inout  wire [15:0] bus_data

'''示例 2：信号位宽声明对齐（执行 "VL: Align signal bit-width"）'''

对齐前：

  reg [7:0]   addr;
  reg [31:0]  data;
  reg [1:0]   state;
  reg [15:0]  counter;

对齐后：

  reg [7:0]  addr;
  reg [31:0] data;
  reg [1:0]  state;
  reg [15:0] counter;

'''示例 3：模块实例化对齐（执行 "VL: Align module instances"）'''

对齐前：

  adder u_adder (
      .clk(clk),
      .rst_n(rst_n),
      .a(a_in),
      .b(b_in),
      .sum(sum_out)
  );

对齐后：

  adder u_adder (
      .clk  (clk),
      .rst_n(rst_n),
      .a    (a_in),
      .b    (b_in),
      .sum  (sum_out)
  );

'''示例 4：阻塞与非阻塞赋值对齐'''

always_comb 中执行 "VL: Align blocking assigns"：

  a_next     = a;
  b_next     = b;
  sum_next   = a + b;
  valid_next = 1'b1;

always_ff 中执行 "VL: Align by non-blocking assign"：

  a     <= a_next;
  b     <= b_next;
  sum   <= sum_next;
  valid <= valid_next;

'''示例 5：行内注释对齐（执行 "VL: Align inline comments"）'''

  wire clk;     // 系统时钟
  wire rst_n;   // 异步复位，低有效
  wire start;   // 启动信号
  wire done;    // 完成标志

=== 使用方式 ===

* '''右键菜单''' → 展开 "Verilog/SV Align" 子菜单，选择需要的对齐命令
* '''命令面板''' → '''Ctrl+Shift+P''' → 输入 "VL:" 查看所有 Verilog/SV 对齐命令
* '''Align by key 按键模式''' → 在设置面板中将常用对齐配置到按键上

=== Verilog/SV 文件类型配置 ===

在选项面板（命令面板 → "Code Alignment: Options"）的 "Auto Selection" 区域编辑 "Verilog/SV File Types"：

默认支持：<code>.v</code> | <code>.sv</code> | <code>.vh</code> | <code>.svh</code> | <code>.vhd</code> | <code>.svp</code>

== 核心功能 ==

=== 对齐命令一览 ===

{| class="wikitable"
! 命令名称
! 功能说明
! 默认快捷键
|-
| '''Align by...'''
| 输入自定义分隔符字符串进行对齐
| Ctrl+Shift+=
|-
| '''Align by equals'''
| 按单个等号 <code>=</code> 对齐
| Ctrl+Shift+1
|-
| '''Align by equals equals'''
| 按双等号 <code>==</code> 对齐
| Ctrl+Shift+2
|-
| '''Align by m_'''
| 按成员变量前缀 <code>m_</code> 对齐
| Ctrl+Shift+3
|-
| '''Align by quote'''
| 按双引号 <code>"</code> 对齐
| Ctrl+Shift+4
|-
| '''Align by period'''
| 按句点 <code>.</code> 对齐
| Ctrl+Shift+5
|-
| '''Align by space'''
| 按空格对齐
| Ctrl+Shift+6
|-
| '''Align from caret'''
| 从光标位置开始对齐（弹出分隔符输入框）
| Ctrl+Shift+7
|-
| '''Align by key'''
| 按键对齐模式（交互式快捷对齐）
| Ctrl+=
|}

=== 功能入口 ===

您可以通过以下四种方式使用 Code Alignment：

* '''命令面板'''：按 '''Ctrl+Shift+P'''，输入 "Align" 即可看到所有对齐命令
* '''右键菜单'''：在编辑器任意位置右键，展开 "Code Alignment" 菜单组（含 "Verilog/SV Align" 子菜单）
* '''状态栏按钮'''：编辑器底部状态栏右侧有 "Align" 快捷按钮，点击即打开分隔符输入框
* '''键盘快捷键'''：使用上表中的默认快捷键直接触发对齐操作（可在 VSCode 键盘快捷方式设置中自定义）

=== 更多对齐示例 ===

'''按 <code>.</code> 对齐（常用于对象属性访问链）：'''

  someObject
      .Property1
      .AnotherProperty
      .FinalProperty;

'''按 <code>m_</code> 对齐（常用于类成员变量声明）：'''

  private string m_name;
  private int    m_age;
  private bool   m_isActive;

'''按 <code>=></code> 对齐（Lambda 表达式 / 箭头函数）：'''

  const add      = (a, b) => a + b;
  const multiply = (a, b) => a * b;
  const divide   = (a, b) => a / b;

== 选项设置 ==

=== 打开设置面板 ===

通过以下方式打开选项面板：
* 命令面板输入 '''Code Alignment: Options'''
* 右键菜单中点击对应选项

设置面板包含三个配置区域：

=== 通用设置 ===

* '''Use IDE tab settings for alignments（使用 IDE 的 Tab 设置）'''：勾选后，对齐操作将遵循 VSCode 当前编辑器的 Tab 宽度和是否用空格替代制表符等设置
* '''Clear most recently used alignment list（清除最近使用的对齐列表）'''：清空 "Align by..." 输入框的下拉历史记录
* '''Backup / Restore settings（备份 / 恢复设置）'''：将当前设置导出为 XML 文件，或从 XML 文件导入设置

=== 键盘快捷键 ===

在此配置 "Align by key" 模式中每个按键对应的对齐行为：

* '''Key（按键）'''：触发对齐的按键，支持所有字母键、数字键和特殊符号键（如 =、Space、.、" 等）
* '''Alignment（对齐字符串）'''：该按键对应的分隔符，例如按键 M 对应 <code>m_</code>
* '''Language（限定语言）'''：如果只希望在特定语言文件中生效，填写文件扩展名（如 <code>.cs</code>）；留空则对所有语言生效
* '''From Caret（从光标开始）'''：勾选后该按键对齐时从光标位置而非行首开始查找分隔符
* '''Is Regex（正则表达式）'''：勾选后将对齐字符串作为正则表达式处理，支持命名分组 <code>(?&lt;compare&gt;...)</code> 和 <code>(?&lt;insert&gt;...)</code>
* '''Add Space（添加空格）'''：勾选后在对齐位置前自动添加一个空格（如果原位置没有空格）

'''恢复默认快捷键'''：点击面板中的 "Restore Defaults" 按钮可恢复预设的五个快捷方式：

{| class="wikitable"
! 按键
! 对齐内容
|-
| =
| =
|-
| 空格
| （空格）
|-
| .
| .
|-
| "
| "
|-
| M
| m_
|}

=== 自动选区设置 ===

配置代码对齐时如何自动确定需要对齐的代码块范围：

* '''XML File Types（XML 文件类型）'''：指定哪些文件扩展名应使用 XML 作用域选择器（每行一个扩展名，如 <code>.xml</code>、<code>.html</code>、<code>.xaml</code>）。在这些文件中，当只有光标没有选区时，会自动按 XML 标签层级智能确定对齐范围
* '''Scope Selector Line Values（作用域选择器行值）'''：空格分隔的值列表，用于匹配"空白行"。例如输入 <code>// { }</code> 会将注释行或括号行也视为边界
* '''Scope Selector Line Ends（作用域选择器行结尾）'''：空格分隔的模式列表，用于匹配行尾特征。例如输入 <code>; { }</code> 会将分号或括号结尾的行也视为边界

'''工作原理'''：当您在没有选区的情况下（光标在某一行的任意位置）执行对齐操作时，扩展会自动向上和向下查找"空白行"作为对齐范围的边界。上述设置可以自定义什么算"空白行"。

== VSCode 设置参考（settings.json）==

您也可以直接在 VSCode 的 <code>settings.json</code> 中手动编辑所有配置项：

<pre>
{
  // 快捷键配置（Align by key 模式使用）
  "codealignment.shortcuts": [
    {
      "value": 187,          // 按键 keyCode（187 = 等号键）
      "alignment": "=",      // 对齐分隔符
      "language": null,      // 限定语言（null = 不限定）
      "alignFromCaret": false,
      "useRegex": false,
      "addSpace": false
    },
    {
      "value": 32,           // 32 = 空格键
      "alignment": " ",
      "language": null,
      "alignFromCaret": false,
      "useRegex": false,
      "addSpace": false
    }
  ],

  // XML 文件类型扩展名列表
  "codealignment.xmlTypes": [
    ".xml", ".html", ".xaml",
    ".cshtml", ".config", ".resx"
  ],

  // 作用域选择器
  "codealignment.scopeSelectorLineValues": "",
  "codealignment.scopeSelectorLineEnds": "",

  // 使用 IDE Tab 设置
  "codealignment.useIdeTabSettings": true,

  // 最近使用的分隔符历史
  "codealignment.delimiters": ["=", "==", "."]
}
</pre>

== 常见问题 ==

=== 快捷键与其他扩展冲突怎么办？ ===
在 VSCode 中按 '''Ctrl+K Ctrl+S''' 打开"键盘快捷方式"面板，搜索 "codealignment"，可以重新绑定所有命令的快捷键。

=== 如何只对选中的行进行对齐？ ===
直接用鼠标或键盘选中需要对齐的行（可以是多行，也可以是非连续的多选区），然后执行任意对齐命令。如果只选中了一行或没有选中，扩展会自动根据上下文智能确定对齐范围。

=== 正则表达式对齐怎么用？ ===
在 "Align by..." 对话框中勾选 "Use Regular Expressions"，或在按键快捷方式设置中勾选 "Is Regex"。分隔符支持标准正则语法，同时支持两个特殊命名分组：

* <code>(?&lt;compare&gt;...)</code> — 指定用于计算对齐位置的参考点
* <code>(?&lt;insert&gt;...)</code> — 指定空格实际插入的位置

例如，使用正则 <code>\s+(?&lt;compare&gt;=)</code> 可以在等号前按空格对齐，而 <code>(?&lt;insert&gt;=)\s*</code> 可以在等号后对齐。

=== 如何修改默认的 XML 文件类型列表？ ===
打开选项面板（命令面板 → "Code Alignment: Options"），在 "Auto Selection" 区域修改 "XML File Types" 文本框，每行一个文件扩展名。

== 技术架构 ==

本扩展完全基于原 Code Alignment (C#/.NET) 项目的架构和算法，使用 TypeScript 重新实现：

<pre>
src/
├── business/                    # 核心业务逻辑（与原项目架构完全对应）
│   ├── Alignment.ts            # 对齐算法主类
│   ├── Key.ts                  # 按键枚举定义
│   ├── KeyShortcut.ts          # 快捷键数据模型
│   ├── LineDetails.ts          # 行细节计算
│   ├── Options.ts              # 设置管理
│   ├── delimiterFinders/       # 分隔符查找器
│   │   ├── NormalDelimiterFinder.ts   # 普通字符串查找
│   │   └── RegexDelimiterFinder.ts    # 正则表达式查找
│   ├── interfaces/             # 接口定义
│   │   ├── IDocument.ts        # 文档抽象接口
│   │   ├── IEdit.ts            # 编辑操作接口
│   │   ├── ILine.ts            # 行数据接口
│   │   ├── IScopeSelector.ts   # 范围选择器接口
│   │   └── IDelimiterFinder.ts # 分隔符查找器接口
│   └── selectors/              # 作用域选择器
│       ├── GeneralScopeSelector.ts # 通用作用域选择器
│       └── XmlScopeSelector.ts     # XML 作用域选择器
├── AlignFunctions.ts           # 对齐功能协调器
├── VSCodeDocument.ts           # VSCode 编辑器文档适配层
└── extension.ts                # 扩展入口与 UI 层
</pre>

== 原功能完整对照 ==

{| class="wikitable"
! 原软件功能
! VSCode 实现方式
|-
| Align by...（自定义分隔符对话框）
| QuickPick 输入面板 + 历史记录
|-
| Align by =、==、m_、"、.、空格
| 独立命令 + 右键菜单 + 快捷键
|-
| Align from caret（从光标位置对齐）
| 命令 + 快捷键 Ctrl+Shift+7
|-
| Align by key（按键截取模式）
| QuickPick 交互式链式对齐面板
|-
| 选项设置面板（通用/快捷键/自动选区）
| WebView 设置面板（三区块配置）
|-
| 快捷键列表编辑
| WebView 表格实时编辑
|-
| XML 作用域选择器
| 算法完整移植，支持 XML 标签边界检测
|-
| 正则表达式分隔符
| 命名分组 (compare/insert) 完整支持
|-
| Tab/空格智能转换
| 适配 VSCode Tab/InsertSpaces 设置
|-
| 右键上下文菜单
| editor/context 菜单贡献
|-
| 状态栏快捷按钮
| StatusBarItem
|-
| 设置备份/恢复
| settings.json 手动管理
|}

== 许可证 ==

与原 Code Alignment 项目使用相同的开源许可证。详见 <code>license.txt</code> 文件。

== 项目链接 ==

* 官方网站：http://www.codealignment.com
* 原始项目（GitHub）：https://github.com/cpmcgrath/codealignment
